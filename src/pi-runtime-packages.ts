/**
 * Pi compatibility packages supplied by Automode when the launching Pi manifest omits them.
 * Their dependencies stay bound to the launching Pi through the runtime loader.
 */
export const SNAPSHOTTED_PI_PACKAGES = Object.freeze([
  "@earendil-works/pi-server",
]);
